services:
  bluetooth-receiver:
    image: pinkynrg/bluetooth-receiver:latest
    container_name: bluetooth-receiver
    network_mode: host
    privileged: true
    volumes:
      - /tmp:/tmp
    environment:
      - DEVICE_NAME=Snapcast Receiver
    restart: always
